<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}{{ site.title or "Mark Drew on Code" }}</title>
    <meta name="description" content="{{ description or site.description or 'A compendium of CFML and web technology topics' }}">
    <link rel="icon" href="/favicon.ico">
    
    <style>
        :root {
            --bg-color: #0a0a0a;
            --text-color: #00ff41;
            --accent-color: #ff6b35;
            --secondary-color: #00ccff;
            --border-color: #333;
            --code-bg: #1a1a1a;
            --link-color: #00ccff;
            --link-hover: #ff6b35;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', monospace;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            text-align: center;
            border-bottom: 2px solid var(--border-color);
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .site-title {
            font-size: 2.5rem;
            color: var(--accent-color);
            margin: 0;
            text-shadow: 0 0 10px var(--accent-color);
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 10px var(--accent-color); }
            to { text-shadow: 0 0 20px var(--accent-color), 0 0 30px var(--accent-color); }
        }

        .site-description {
            color: var(--secondary-color);
            margin: 10px 0 0 0;
            font-size: 1.1rem;
        }

        nav {
            margin: 20px 0;
            text-align: center;
        }

        nav a {
            color: var(--link-color);
            text-decoration: none;
            margin: 0 15px;
            padding: 5px 10px;
            border: 1px solid var(--link-color);
            transition: all 0.3s ease;
        }

        nav a:hover {
            background-color: var(--link-color);
            color: var(--bg-color);
            box-shadow: 0 0 10px var(--link-color);
        }

        main {
            min-height: 60vh;
        }

        h1, h2, h3, h4, h5, h6 {
            color: var(--accent-color);
        }

        h1 {
            font-size: 2rem;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 10px;
        }

        a {
            color: var(--link-color);
            text-decoration: underline;
        }

        a:hover {
            color: var(--link-hover);
            text-shadow: 0 0 5px var(--link-hover);
        }

        code {
            background-color: var(--code-bg);
            padding: 2px 4px;
            border-radius: 3px;
            font-family: 'Courier New', monospace;
        }

        pre {
            background-color: var(--code-bg);
            padding: 15px;
            border-radius: 5px;
            overflow-x: auto;
            border: 1px solid var(--border-color);
        }
        
        pre code {
            background: none;
            padding: 0;
            border-radius: 0;
            font-size: inherit;
        }
        
        /* Fix extra whitespace in code blocks */
        pre {
            white-space: pre;
        }
        
        pre code {
            display: block;
            white-space: pre;
        }

        blockquote {
            border-left: 4px solid var(--accent-color);
            margin: 0;
            padding: 0 20px;
            font-style: italic;
            color: #ccc;
        }

        .post-meta {
            color: var(--secondary-color);
            font-size: 0.9rem;
            margin-bottom: 20px;
        }

        .post-list {
            list-style: none;
            padding: 0;
        }

        .post-item {
            margin-bottom: 30px;
            padding: 20px;
            border: 1px solid var(--border-color);
            border-radius: 5px;
            transition: border-color 0.3s ease;
        }

        .post-item:hover {
            border-color: var(--accent-color);
        }

        .post-title {
            margin: 0 0 10px 0;
            font-size: 1.5rem;
        }

        .post-title a {
            color: var(--accent-color);
            text-decoration: none;
        }

        .post-title a:hover {
            text-shadow: 0 0 10px var(--accent-color);
        }

        .post-excerpt {
            margin: 10px 0;
            color: #ccc;
        }

        footer {
            margin-top: 50px;
            padding: 20px 0;
            border-top: 2px solid var(--border-color);
            text-align: center;
            color: var(--secondary-color);
        }

        .social-links {
            margin: 20px 0;
        }

        .social-links a {
            margin: 0 10px;
            color: var(--link-color);
            text-decoration: none;
            padding: 5px 10px;
            border: 1px solid var(--link-color);
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            background-color: var(--link-color);
            color: var(--bg-color);
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .container {
                padding: 10px;
            }
            
            .site-title {
                font-size: 2rem;
            }
            
            nav a {
                display: block;
                margin: 5px 0;
            }
        }

        /* Post content styling */
        .post-content img {
            max-width: 100%;
            height: auto;
            border: 1px solid var(--border-color);
            margin: 20px 0;
        }
        
        .post-subtitle {
            color: var(--secondary-color);
            font-style: italic;
            margin-bottom: 10px;
        }
        
        .read-more {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: bold;
        }
        
        .read-more:hover {
            text-shadow: 0 0 5px var(--accent-color);
        }
        
        .back-to-home {
            color: var(--link-color);
            text-decoration: none;
            padding: 10px 15px;
            border: 1px solid var(--link-color);
            transition: all 0.3s ease;
        }
        
        .back-to-home:hover {
            background-color: var(--link-color);
            color: var(--bg-color);
        }
        
        .view-all-posts {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
        }
        
        .view-all-posts:hover {
            text-shadow: 0 0 10px var(--accent-color);
        }
        
        .all-posts {
            text-align: center;
            margin-top: 40px;
        }

        /* Syntax highlighting overrides */
        .highlight {
            background-color: var(--code-bg) !important;
        }
        
        .highlight pre {
            background-color: var(--code-bg) !important;
            color: var(--text-color) !important;
        }
        
        /* Archive page styling */
        .posts-archive {
            max-width: 1000px;
        }
        
        .archive-summary {
            color: var(--secondary-color);
            font-size: 1.1rem;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        .year-navigation {
            margin: 2rem 0;
            padding: 1rem;
            border: 1px solid var(--border-color);
            border-radius: 5px;
            background-color: rgba(0, 255, 65, 0.05);
        }
        
        .year-navigation h3 {
            margin: 0 0 1rem 0;
            color: var(--accent-color);
        }
        
        .year-links {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        
        .year-link {
            display: inline-block;
            padding: 8px 15px;
            background-color: var(--code-bg);
            color: var(--link-color);
            text-decoration: none;
            border: 1px solid var(--border-color);
            border-radius: 3px;
            transition: all 0.3s ease;
        }
        
        .year-link:hover {
            background-color: var(--link-color);
            color: var(--bg-color);
            box-shadow: 0 0 10px var(--link-color);
        }
        
        .year-section {
            margin: 3rem 0;
            padding: 2rem 0;
            border-top: 2px solid var(--border-color);
        }
        
        .year-heading {
            color: var(--accent-color);
            font-size: 2rem;
            margin-bottom: 1.5rem;
            text-shadow: 0 0 10px var(--accent-color);
        }
        
        .posts-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 1.5rem;
        }
        
        .archive-post-item {
            padding: 1rem;
            border: 1px solid var(--border-color);
            border-radius: 5px;
            background-color: rgba(0, 255, 65, 0.02);
            transition: all 0.3s ease;
        }
        
        .archive-post-item:hover {
            border-color: var(--accent-color);
            background-color: rgba(255, 107, 53, 0.05);
        }
        
        .archive-post-title {
            margin: 0 0 0.5rem 0;
            font-size: 1.1rem;
        }
        
        .archive-post-title a {
            color: var(--link-color);
            text-decoration: none;
        }
        
        .archive-post-title a:hover {
            color: var(--accent-color);
            text-shadow: 0 0 5px var(--accent-color);
        }
        
        .archive-post-subtitle {
            margin: 0 0 0.5rem 0;
            font-size: 0.9rem;
            color: var(--secondary-color);
            font-style: italic;
        }
        
        .archive-post-meta {
            font-size: 0.8rem;
            color: var(--secondary-color);
            opacity: 0.8;
        }
        
        .categories {
            margin-left: 0.5rem;
            font-style: italic;
        }
        
        .back-to-top {
            text-align: center;
            margin: 3rem 0 2rem 0;
        }
        
        .back-to-top a {
            color: var(--accent-color);
            text-decoration: none;
            padding: 10px 20px;
            border: 1px solid var(--accent-color);
            border-radius: 5px;
            transition: all 0.3s ease;
        }
        
        .back-to-top a:hover {
            background-color: var(--accent-color);
            color: var(--bg-color);
            box-shadow: 0 0 10px var(--accent-color);
        }
        
        /* Responsive archive */
        @media (max-width: 600px) {
            .posts-grid {
                grid-template-columns: 1fr;
            }
            
            .year-links {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1 class="site-title">Mark Drew on Code</h1>
            <p class="site-description">A compendium of CFML and web technology topics</p>
            <nav>
                <a href="/">Home</a>
                <a href="/posts/">All Posts</a>
                <a href="/about/">About</a>
            </nav>
        </header>

        <main>
            {{ content | safe }}
        </main>

        <footer>
            <div class="social-links">
                <a href="https://github.com/{{ site.author.github }}" target="_blank">GitHub</a>
                <a href="https://www.linkedin.com/in/{{ site.author.linkedin }}" target="_blank">Twitter</a>
                <a href="https://instagram.com/{{ site.author.instagram }}" target="_blank">Instagram</a>
                <a href="https://x.com/{{ site.author.twitter }}" target="_blank">X</a>
                <a href="https://bsky.app/profile/{{ site.author.bluesky }}" target="_blank">BlueSky</a>
                <a href="https://tiktok.com/@{{ site.author.tiktok }}" target="_blank">TikTok</a>
                <a href="mailto:{{ site.author.email }}">Email</a>
            </div>
            <p>&copy; 2025 {{ site.author.name }}. All rights reserved.</p>
        </footer>
    </div>
</body>
</html>

