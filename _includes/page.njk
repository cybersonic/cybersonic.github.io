---
layout: base.njk
---

<div class="page-content">
    <h1>{{ title }}</h1>
    
    {% if description %}
    <p class="page-description">{{ description }}</p>
    {% endif %}
    
    <div class="intro">
        <p>Welcome to my blog!</p>
        <p>Here you will find posts about various topics including ColdFusion, Railo/Lucee, MongoDB, Node, Meteor, CFML, Apple hardware and software, and web development.</p>
        <p>I strive to keep the content original and informative.</p>
    </div>

    <h2>Recent Posts</h2>
    <div class="post-list">
        {%- for post in collections.posts | slice(0, 10) -%}
        <article class="post-item">
            <h3 class="post-title">
                <a href="{{ post.url }}">{{ post.data.title }}</a>
            </h3>
            {% if post.data.subtitle %}
            <p class="post-subtitle">{{ post.data.subtitle }}</p>
            {% endif %}
            <div class="post-meta">
                Posted on {{ post.date | dateDisplay }}
                {% if post.data.categories %}
                in {{ post.data.categories }}
                {% endif %}
            </div>
            {% if post.data.excerpt %}
            <div class="post-excerpt">
                {{ post.data.excerpt }}
            </div>
            {% endif %}
        </article>
        {%- endfor -%}
    </div>
</div>
